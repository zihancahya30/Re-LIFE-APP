<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.RE_LIFE"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.RE_LIFE">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register"/>
        <activity
            android:theme="@style/Theme.AppCompat.Light"
            android:name=".RegisterCustomer"/>
        <activity
            android:theme="@style/Theme.AppCompat.Light"
            android:name=".RegisterService"/>
        <activity android:name=".LoginCustomer"/>
        <activity android:name=".LoginService"/>
        <activity
            android:name=".HomeService"
            android:theme="@style/Theme.AppCompat.Light">
        </activity>
        <activity android:name=".HomeCustomer"
            android:theme="@style/Theme.AppCompat.Light"/>
        <activity android:name=".PengelolaanService"/>
        <activity android:name=".ActivityService"/>
        <activity android:name=".ProfileService"/>
        <activity android:name=".EditProfileService"/>
        <activity android:name=".TipsDaurCustomer"/>
        <activity android:name=".TipsNonDaurCustomer"/>
    </application>
</manifest>