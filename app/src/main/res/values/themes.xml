<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.RE_LIFE" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Warna utama tema -->
        <item name="colorPrimary">@color/hijau_gelap</item>
        <item name="colorPrimaryVariant">@color/hijau_gelap</item>
        <item name="colorSecondary">@color/hijau_gelap</item>

        <!-- Warna tombol default -->
        <item name="colorButtonNormal">@color/hijau_gelap</item>
        <item name="android:button">@color/hijau_gelap</item>
        <item name="android:textColor">@android:color/black</item>

        <!-- Gaya tombol -->
        <item name="buttonStyle">@style/Widget.MaterialComponents.Button</item>
    </style>
</resources>
